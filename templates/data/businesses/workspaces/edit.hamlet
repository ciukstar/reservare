
<header>
  <md-icon-button href=@{DataR $ WorkspaceR bid wid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgWorkspace}

<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ WorkspaceR bid wid} enctype=#{et} novalidate ##{idFormWorkspace}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@{DataR $ WorkspaceR bid wid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
