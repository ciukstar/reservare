
<header.background>
  <md-icon-button href=@?{(DataR $ BusinessServiceR bid sid,stati)} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgService}

<main>

  ^{widgetBanner msgs}

  <form method=post action=@?{(DataR $ BusinessServiceR bid sid,stati)} enctype=#{et} novalidate ##{idFormService}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@?{(DataR $ BusinessServiceR bid sid,stati)}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

  ^{widgetSnackbar msgs}
