
<header>
  <md-icon-button href=@{DataR $ PayOptionR bid wid oid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgPaymentOption}

<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ PayOptionR bid wid oid} enctype=#{et} novalidate ##{idFormPayOption}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@{DataR $ PayOptionR bid wid oid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
