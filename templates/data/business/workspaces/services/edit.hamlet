
<header.fixed ##{idHeader}>
  <nav>
    <a.button.circle.transparent href=@?{(DataR $ WorkspaceServiceR bid wid sid,stati)} role=button title=_{MsgBack}>
      <i>arrow_back

    <h6.max>
      _{MsgService}

<main.responsive ##{idMain}>

  <form method=post action=@?{(DataR $ WorkspaceServiceR bid wid sid,stati)} enctype=#{et} novalidate ##{idFormService}>
    ^{fw}
    <nav.right-align>
      <a.button.transparent.link href=@?{(DataR $ WorkspaceServiceR bid wid sid,stati)} role=button>
        _{MsgCancel}
        
      <button type=submit>
        _{MsgSave}

  ^{widgetSnackbar msgs}
