
<header>
  <md-icon-button href=@{DataR $ ServiceAssignmentsR sid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgServiceAssignment}

<main>

  <form method=post action=@{DataR $ ServiceAssignmentsR sid} enctype=#{et} novalidate ##{idFormAssignment}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@{DataR $ ServiceAssignmentsR sid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
