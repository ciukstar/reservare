
<header>
  <md-icon-button href=@{DataR $ ServiceR sid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgService}

<main>

  ^{widgetBanner msgs}

  <form method=post action=@{DataR $ ServiceR sid} enctype=#{et} novalidate ##{idFormStaff}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@{DataR $ ServiceR sid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
