
<header>
  <md-icon-button href=@{DataR $ UserR uid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgUser}
      
<main>

  <form method=post action=@{DataR $ UserR uid} enctype=#{et} novalidate>
    ^{fw}
    <div.app-form-actions>
      <md-text-button type=button href=@{DataR $ UserR uid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}
