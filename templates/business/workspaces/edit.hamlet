
<header>
  <md-icon-button href=@{WorkspaceR uid bid wid} aria-label=_{MsgBack}>
    <md-icon>arrow_back

  <span.md-typescale-title-large>
    _{MsgWorkspace}

<main>

  <form method=post action=@{WorkspaceR uid bid wid} enctype=#{et} novalidate ##{idFormWorkspace}>
    ^{fw}
    <div.form-actions>
      <md-text-button type=button href=@{WorkspaceR uid bid wid}>
        _{MsgCancel}
      <md-filled-button type=submit>
        _{MsgSave}

^{widgetSnackbar msgs}
