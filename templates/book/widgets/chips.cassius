
##{idFilterChips}
    display: flex
    flex-direction: column

    details
        summary
            position: relative
            padding: 1rem
            display: flex
            align-items: center
            justify-content: flex-start
            .#{classSummaryLabel}
                margin-inline-start: 1rem
                margin-inline-end: auto

    details[open] > summary > .#{classSummaryEndIcon}
        transform: rotate(180deg)